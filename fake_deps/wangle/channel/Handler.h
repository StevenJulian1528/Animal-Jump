#pragma once
#include "folly/io/IOBufQueue.h"

namespace wangle {
    // Forward decls that match wangle/channel/Handler.h expectations if needed
    // But we defined InboundHandler in folly_mock.h for simplicity
}
